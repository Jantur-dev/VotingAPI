const mix = require('laravel-mix');

mix.webpackConfig({
  module: {
    rules: [
      {
        test: /\.m?js$/,
        resolve: {
          fullySpecified: false,
        },
      },
    ],
  },
});
